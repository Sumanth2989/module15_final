"""
Profile router removed.

This module previously provided user profile and password-change endpoints.
The feature was removed to avoid DB schema drift (username column) and simplify the app.

No routes are exported here anymore.
"""
